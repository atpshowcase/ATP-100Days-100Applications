# Digital Clock (Next.js)

A small Next.js app that displays a live digital clock.

## Quick start ✅

1. Install dependencies:

```bash
npm install
```

2. Run in development:

```bash
npm run dev
```

3. Open http://localhost:3000 in your browser.

---

If you'd like, I can run `npm install` and `npm run dev` for you now. Want me to start the dev server? 🔧