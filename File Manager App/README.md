# File Manager App

Simple File Manager built with Next.js (App Router) and Tailwind CSS v3.

## Setup

1. Install dependencies:

```bash
npm install
```

2. Run dev server:

```bash
npm run dev
```

Files are stored in the `uploads/` directory at project root.
